# Exemplo de variáveis de ambiente para o frontend (Vite)
# URL base da API (use http://localhost:8080 quando a API estiver no Docker local)
VITE_API_BASE_URL=http://localhost:8080

# Porta para rodar o dev server (opcional)
VITE_PORT=5173
